import React from 'react'
import { Metadata } from 'next';

const Orders = () => {
  return (
    <div>Orders page</div>
  )
}

export const metadata: Metadata = {
  title: 'Orders',
};

export default Orders